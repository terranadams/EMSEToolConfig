if (inspResult=="Ok to Issue") {
        cancel = true;
        showMessage = true;
        comment("<b>Please enter an Inspection Comment then re-submit</b>");
    };

showDebug=true;

logDebug("in InspectionResultModifyBefore");